/// <reference path="../../../node_modules/@types/jasmine/index.d.ts" />
import { TestBed, async, ComponentFixture, ComponentFixtureAutoDetect } from '@angular/core/testing';
import { BrowserModule, By } from "@angular/platform-browser";
import { PocoSearchFilterComponent } from './poco_search_filter.component';

let component: PocoSearchFilterComponent;
let fixture: ComponentFixture<PocoSearchFilterComponent>;

describe('poco_create component', () => {
    // beforeEach(async(() => {
    //     TestBed.configureTestingModule({
    //         declarations: [PocoSearchFilterComponent],
    //         imports: [ BrowserModule ],
    //         providers: [
    //             { provide: ComponentFixtureAutoDetect, useValue: true }
    //         ]
    //     });
    //     fixture = TestBed.createComponent(PocoSearchFilterComponent);
    //     component = fixture.componentInstance;
    // }));

    // it('should do something', async(() => {
    //     expect(true).toEqual(true);
    // }));
});
